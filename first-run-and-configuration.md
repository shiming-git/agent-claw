# 首次运行与配置

本文档说明在首次启动 `agent-claw` 后需要完成的配置。

## 1) 找到配置文件

发布包同级目录下有 `config.json`，按需修改后**保存**（多数情况下保存后需**重启程序**才会生效）。

## 2) 配置访问 Token（必填）

- `service_token`：用于访问鉴权
  - 首次打开 Web 页面时会提示输入 Token
  - 输入正确后才能进入系统

## 3) 接入大模型 API（必填）

平台支持接入多家不同的大模型 API：**只要接口兼容 OpenAI API 形式**，通常都可以接入。

在 `models[]` 中添加一条或多条模型信息：

- `base_url`：模型服务的 API 地址（端点）
- `model_name`：模型名称或标识
- `api_key`：访问密钥

可配置多组模型，后续在不同 Agent 或不同场景下按需选用。

## 4) 常见问题

- **Token 校验失败**：核对 `config.json` 中的 `service_token` 与页面输入是否一致；必要时重启程序。
- **模型调用失败**：检查 `base_url` 是否可访问、`api_key` 是否有效、`model_name` 是否正确；若服务商还需额外设置，请先在服务商侧完成配置。
